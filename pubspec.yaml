name: test_1
description: '生理期记录工具'
publish_to: 'none'
version: 0.3.3

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  sqflite: ^2.3.3
  path: ^1.8.3
  flutter_localizations:
    sdk: flutter
  local_auth: ^2.3.0
  local_auth_android: ^1.0.49

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
